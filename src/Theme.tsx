import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#ff9e3c', // Accent color
    },
    background: {
      default: '#e6e6e6',
      paper: '#F5F5F5',
    },
  },
});

export default theme;